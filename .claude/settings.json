{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -E '\\.(js|jsx)$' | { read -r f; [ -n \"$f\" ] && cd /Users/josephcalvey/Documents/my-portfolio && npx eslint --no-warn-ignored \"$f\" 2>&1 || true; } 2>/dev/null || true",
            "statusMessage": "Linting..."
          }
        ]
      }
    ]
  }
}
